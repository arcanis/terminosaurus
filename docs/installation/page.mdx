---
title: Installation
nextjs:
  metadata:
    title: Installation
    description: Quidem magni aut exercitationem maxime rerum eos.
---

Create a new project:

```bash
yarn init -2
```

Add the `terminosaurus` package:

```bash
yarn add terminosaurus
```

And you're ready to go! Terminosaurus doesn't require anything else.
